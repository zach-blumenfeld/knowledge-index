# CLAUDE.md

Claude Code: **at the start of every session, read [`AGENTS.md`](AGENTS.md) end-to-end before doing anything substantial.** It's the canonical contributor guide — design principles, project map, conventions, the *Don't* list — and all of it applies to Claude Code. Don't ask the user "want me to look around?" — just read it.

This file exists only because Claude Code auto-loads `CLAUDE.md`. `AGENTS.md` is the single source of truth; nothing is duplicated here.
